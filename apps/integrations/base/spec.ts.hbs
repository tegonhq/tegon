export function spec() {
  return {
    workspace_auth: {
      OAuth2: {
        // OAuth information
      },
    },
    // Remove this if there is no personal account connection workflow
    personal_auth: {
      OAuth2: {
        // OAuth information to connect personal accounts
      },
    },
  };
}
